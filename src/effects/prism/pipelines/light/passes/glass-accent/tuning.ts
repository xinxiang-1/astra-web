/** Light-only contrast layer applied after the shared physical front glass. */
export const LIGHT_GLASS_ACCENT_TUNING = Object.freeze({
  bandCenter: 0.052,
  bandWidth: 0.034,
  bandStrength: 0.52,
  baseReflection: 0.05,
  rimStrength: 0.45,
  baseRimStrength: 0.28,
  environmentLodBias: 1.6,
  highlightStrength: 0.95,
});
