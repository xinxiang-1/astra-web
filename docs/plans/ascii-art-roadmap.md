# 图片转字符画 · 优化方向建议

> 面向当前 `/ascii-art` 能力：灰度字符、文字铺底、彩色预览、TXT/PNG 导出、主题自适应预览。  
> 原则：**先让「第一次就好看、愿意发出去」**，再做动态与后端。

---

## 1. 现状与定位

### 已具备

| 能力 | 说明 |
|------|------|
| 灰度字符 | 亮度映射字符集，多档清晰度（含极清） |
| 文字铺底 | 指定文案按明暗轮廓填充（如「我爱你中国」） |
| 彩色预览 | 按原图像素上色，两种模式通用 |
| 导出 | TXT（UTF-8 BOM、正向）、PNG（白底正向） |
| 预览体验 | 自适应缩放、Ctrl+滚轮、全屏、主题正反向 |

### 建议产品定位

- **主记忆点**：中文铺字 + 彩色字符画（可晒、可表白），而不只是「又一个 ASCII 工具」。
- **信任点**：纯前端、图片/视频不上传服务器。
- **技术路线**：静态与动态预览先前端；服务端出片/存作品靠后。

---

## 2. 体验优化方向（P0）

目标：降低「不会用 / 调参慌 / 导出懵」三类摩擦。

### 2.1 首屏示范

- 提供 1–2 张内置示例图（人像、风景各一）。
- 「试试这张」一点即出效果，教会用户什么图适合。
- 空白态文案从「请上传」改为「上传或先试示例」。

### 2.2 风格预设（外露，高级内收）

在主操作区增加少量预设，一点改齐模式与关键参数，例如：

| 预设 | 意图 |
|------|------|
| 清晰单色 | 灰度 + 关彩色 + 高清 |
| 彩色 dens | 灰度 + 开彩色 + 高清/超清 |
| 告白铺字 | 文字铺底 + 默认文案 + 彩色 + 合适阈值 |

高级设置保留给深度用户；预设覆盖 80% 场景。

### 2.3 导出说明更清楚

- 下载旁固定提示：「导出为白底正向，记事本可直接打开」。
- 彩色开启时：PNG 标注「彩色图」；关闭时标注「单色字」。
- 避免用户误以为「预览深色 = 导出也是深色」。

### 2.4 重计算反馈

- 极清 + 彩色时显示明确「生成中」状态（按钮/预览区）。
- meta 可补充「已自适应缩至预览区」，减少「怎么这么小」的疑惑。

### 2.5 文字铺底引导

- 文案输入旁增加快捷芯片：表白、名字占位、节日等。
- 阈值/曝光与「铺满整图」用一句话解释结果差异（少字轮廓 vs 满屏字）。

### 2.6 已有交互保持

- Ctrl/⌘ + 滚轮缩放、全屏、恢复默认：保留并在首次使用时用短提示带过即可。

---

## 3. 产品能力方向

### 阶段 A · 静态打磨到「能晒」（优先）

- 完成第 2 节体验项。
- 可选：导出图带轻量品牌角标 / 分享构图（仍本地生成）。
- 成功标准：新用户 30 秒内能出一张愿意发朋友圈的图。

### 阶段 B · 动态字符视频（前端）

- 上传短视频或摄像头 → 按帧灰度/彩色字符实时预览。
- 限制列数与帧率档位（如 80–120 列、12–24 fps），保证流畅。
- 可选：`MediaRecorder` 本地录成 WebM/短片下载。
- **暂不做**：长视频高清服务端转码。

成功标准：手机摄像头对着人脸能实时看到字符动态，卡顿可接受。

### 阶段 C · 传播与轻量沉淀

- 分享落地页或「复制分享文案」。
- 本地历史（IndexedDB）最近 N 次参数/缩略图，无账号。
- 水印/角标开关。

### 阶段 D · 后端（有明确需求再上）

仅在出现以下需求时启动：

- 长视频稳定高清导出
- 作品云端保存与账号体系
- 公开广场 / 审核 / 运营配置

---

## 4. 差异化与叙事

| 要强调 | 少强调 |
|--------|--------|
| 用字画出图、表白/节日铺字 | 纯极客「ASCII 终端复古」单一叙事 |
| 本地处理、不上传 | 过早讲社区、积分、会员 |
| 彩色字符画观感 | 无限堆字符集却无预设 |

首页 / 工具页一句话示例：

> 本地图片转字符画：灰度或中文铺字，支持彩色预览，一键导出 TXT / PNG。

---

## 5. 建议不做或暂缓

- 同时大改静态 + 上线完整视频导出管线（易两边半成品）。
- 无预设、无示例时继续加冷门参数。
- 一上来账号、广场、服务端存片。
- 与现有氛围站视觉冲突的「仪表盘式」控件堆叠。

---

## 6. 落地优先级清单

| 序 | 项 | 类型 | 预估 |
|----|----|------|------|
| 1 | 示例图 + 空态引导 | UX | 小 |
| 2 | 三个风格预设 | UX / 产品 | 小 |
| 3 | 导出文案与状态反馈 | UX | 小 |
| 4 | 铺底快捷文案芯片 | UX | 小 |
| 5 | 动态字符预览（视频/摄像头） | 功能 | 中 |
| 6 | 本地录制下载 | 功能 | 中 |
| 7 | 本地历史 / 分享角标 | 产品 | 中 |
| 8 | 后端出片与账号 | 基础设施 | 大 |

---

## 7. 与现有计划文档关系

| 文档 | 角色 |
|------|------|
| [ascii-art.md](./ascii-art.md) | 阶段 1 范围与已完成基线 |
| **本文** | 体验与产品优化方向、后续阶段建议 |

阶段 2（彩色、导出等）主体能力已落地；后续以本文 P0 体验与阶段 B 动态预览为主轴推进。

---

## 8. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-09-15 | 初稿：体验 P0、产品阶段 A–D、优先级清单 |
